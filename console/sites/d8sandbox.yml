local:
  root: /var/www/docroot
  type: local
dev:
  root: 
  host: 
  uri: 
  user: 
  password: 
  type: ssh
  options:
  arguments:
  keys:
      public: ~/.ssh/id_rsa.pub
      private: ~/.ssh/id_rsa
      passphrase: ~/.ssh/passphrase.txt
prod:
  root: /var/www/html/docroot
  host:
  user:
  type: ssh
  options:
  arguments:
  keys:
      public: ~/.ssh/id_rsa.pub
      private: ~/.ssh/id_rsa
      passphrase: ~/.ssh/passphrase.txt

